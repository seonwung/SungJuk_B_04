성적 입력_SD
sequenceDiagram
    autonumber
    actor ProfessorUser as 교수(사용자)
    participant UI as Add_Sungjuk_UI
    participant SJ as 성적
    participant PROF as 교수

    ProfessorUser->>+UI: 성적입력요청()
    UI->>+SJ: 성적입력(교수id, 학생id, 자바, DB, 보안)
    
    SJ->>+PROF: 교수체크(교수id)
    Note over PROF: id가 "inha"인지 확인
    PROF-->>-SJ: boolean 결과
    
    alt 교수체크 성공 (true)
        SJ->>SJ: 총점 및 평균 계산
        SJ-->>UI: 성적 입력 완료 메시지
    else 교수체크 실패 (false)
        SJ-->>-UI: 에러: 교수 권한이 없습니다.
    end
    
    UI-->>-ProfessorUser: 처리 결과 화면 출력